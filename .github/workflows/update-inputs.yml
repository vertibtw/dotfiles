name: Update flake.lock

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0'

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - uses: DeterminateSystems/determinate-nix-action@v3
      - run: git pull
      - run: nix flake update
      - name: Commit and push if changed
        run: |
          git config user.name "vertibtw"
          git config user.email "12345678+vertibtw@users.noreply.github.com"
          git add flake.lock
          git diff --staged --quiet || git commit -m "update"
          git push
