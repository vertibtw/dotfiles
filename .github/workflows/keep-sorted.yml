on:
  push:
    branches:
      - main

jobs:
  sort:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/determinate-nix-action@v3
      - run: git pull
      - run: nix shell nixpkgs#keep-sorted --command bash -c "find . -name '*.nix' | xargs keep-sorted"
      - run: |
          git config user.name "vertibtw"
          git config user.email "279445040+vertibtw@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || git commit -m "keep-sorted"
          git push
